import React from 'react';

const UserCard = () => {
    // const params = useParams();
    // const [userId, setUserId] = useState();
    // useEffect(() => {
    //     api.users.getById(params).then((data) => setUserId(data));
    // }, []);
    // console.log({ userId });
    return (
        <h1>Hello</h1>
    );
};

export default UserCard;
